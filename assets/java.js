function generate_array(game_level){
    
    console.log("hello world");
    var test = document.getElementById('g-level').value;
    alert(test);
    let game_array=[];//might need var rather than let (block v global scope)
    return game_array;
}

